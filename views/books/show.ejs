<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=book.title%></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <div class="book-details">
      <h1><%=book.title%></h1>
      <img src="<%=book.cover%>" alt="" />
      <p>Author: <%=book.author%></p>
      <p>Publication Date: <%=book.publishedDate%></p>
      <p>Synopsis: <%=book.description%></p>
      <p>Genre(s): <%=book.genre%></p>
      <p>Page Count: <%=book.pages%> Pages</p>
      <p>ISBN: <%=book.isbn%></p>
    </div>
    <div class="book-actions">
      <form
        class-="delete-form"
        action="/books/<%=book._id%>?_method=DELETE"
        method="post"
      >
        <button class="delete-button" type="submit">Delete</button>
      </form>

      <a class="edit-link" href="/books/<%=book._id%>/edit">
        <div class="edit-text">Edit</div>
      </a>

      <a class="books-link" href="/books">
        <div class="books-link-text">Back to Books</div>
      </a>
    </div>
  </body>
</html>
